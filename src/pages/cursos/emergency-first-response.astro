---
import { info, cursos } from "../../data/data.json";
import Layout from "../../layouts/Layout.astro";
---

<Layout
  title=`${cursos.efr.name} | ${info.name}`
  description=`Curso ${cursos.efr.name} - ${cursos.efr.description} | ${info.address}, Tel.: ${info.phone}, mail: ${info.email}`
>
  <section>
    <h1>EMERGENCY FIRST RESPONSE</h1>
  </section>
</Layout>
