---
import { info, links } from "../data/data.json";
import { buda } from "@data/buda.json";
import Layout from "../layouts/Layout.astro";
import SectionContainer from "@components/SectionContainer.astro";
import TitlePage from "@components/TitlePage.astro";
import ListItemBuda from "@/components/ListItemBuda.astro";
import LinkButton from "@components/LinkButton.astro";
import WhatsappIcon from "@icons/WhatsappIcon.astro";
import BudaGalleryItem from "@/components/BudaGalleryItem.astro";
---

<Layout
  title=`Checkout ${info.name}`
  description=`Certificate con nosotros en nuestra cantera El Buda a solo 50Km de CABA, Tel.: ${info.phone}, mail: ${info.email}`
  canonicalURL="https://www.divingschool.com.ar/buda"
  ogTitle="¿Querés certificar o practicar buceo?"
  ogDescription="Tenemos nuestro propio lugar para práctica y certificaciones de buceo. Te esperamos!"
>
  <SectionContainer class="pt-3 pb-3">
    <TitlePage
      title="Espejo de agua El Buda"
      subtitle="Nuestro propio lugar para practica y certificaciones de buceo"
    />
  </SectionContainer>

  <SectionContainer class="pt-0 pb-3">
    <article>
      <ul>
        <ListItemBuda> <b>Ubicación:</b> Benavidez, a 50 minutos de CABA. </ListItemBuda>
        <ListItemBuda
          ><b>Más rápido:</b> Sábados y domingos o cualquier día de la semana. Sin necesidad de viajar para certificar.</ListItemBuda
        >
        <ListItemBuda
          ><b>Personalizado:</b> Solo grupos reducidos y con un máximo de dos personas por instructor.</ListItemBuda
        >
        <ListItemBuda> <b>Incluye:</b> Tanques y lastres. </ListItemBuda>
        <ListItemBuda> <b>Profundidad maxima:</b> 12 metros. </ListItemBuda>
      </ul>
      <ul>
        <ListItemBuda
          >Contamos con un recorrido llamado <b>El camino de las viejas</b> por la cantida de viejas del agua que se ven
          y <b>dos plataformas</b> para realizar los ejercicios. La pared sur nos permite hacer un recorrido desde las plataformas
          hasta la playa.</ListItemBuda
        >
        <ListItemBuda><b>Comodidades:</b> Estacionamiento, baños, quinchos, bar, kayaks, etc.</ListItemBuda>
        <ListItemBuda
          >En tu visita está incluído el almuerzo: hamburguesa, papas fritas y bebida sin alcohol. Además de la remera
          de regalo y la bitacora para los que certifican y completan las 4 inmersiones.</ListItemBuda
        >
      </ul>
    </article>

    <div class="button">
      <LinkButton href={links.whatsapp} colorPrimary={true} target="_blank">
        Contacto <WhatsappIcon />
      </LinkButton>
    </div>
  </SectionContainer>

  <SectionContainer class="pt-0 pb-6">
    <div class="buda__gallery">
      {
        buda.map((b) => {
          return <BudaGalleryItem b={b} />;
        })
      }
    </div>
  </SectionContainer>
</Layout>

<style lang="scss">
  @import "@styles/variables";

  article {
    padding-inline: 16px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    gap: 16px;

    ul {
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 16px;
      width: 100%;
    }
  }

  .button {
    display: flex;
    justify-content: center;
    align-items: center;
    margin-block: 48px;
  }

  .buda__gallery {
    display: grid;
    grid-template-columns: repeat(auto-fill, min(100%, 400px));
    gap: 4px;
    width: 100%;
    justify-content: center;
  }

  @media (width > 768px) {
    article {
      padding-inline: 48px;
      flex-direction: row;
      gap: 64px;
    }
  }
</style>
