---
interface Props {
  trip: {
    id: string;
    name: string;
    img: string;
    withTitle: boolean;
    facebookAlbum: string;
  };
}

const { trip } = Astro.props;

const src = `/img/viajes/${trip.img}`;
const alt = `Foto del grupo de buceadores en ${trip.name}`;
const withTitle = trip.withTitle;
const name = trip.name;
---

<article>
  <img src={src} alt={alt} loading="lazy" />
  {
    !withTitle && (
      <div>
        <h2>{name}</h2>
        <h3>
          <span>ATLANTIDA</span> Más que una Escuela de <span>BUCEO</span>
        </h3>
      </div>
    )
  }
</article>

<style lang="scss">
  @import "@styles/variables";

  article {
    position: relative;

    img {
      position: relative;
      display: block;
      width: 100%;
      height: auto;
      aspect-ratio: 4/3;
      object-fit: contain;
      background: #000;
    }

    div {
      position: absolute;
      bottom: 0;
      width: 100%;
      backdrop-filter: blur(1px);

      h2 {
        height: 32px;
        line-height: 34px;
        padding-left: 4px;
        background: linear-gradient(to top, rgba($color-primary, 1) 45%, rgba($color-primary, 0) 100%);
        font-family: $font-family;
        font-weight: 400;
        font-size: 16px;
        text-align: left;
        color: $font-color-min;
        border-bottom: 2px solid $color-body-primary;
      }

      h3 {
        height: 32px;
        padding-left: 4px;
        padding-top: 6px;
        background: linear-gradient(to bottom, rgba($color-secondary, 1) 75%, rgba($color-secondary, 0) 100%);
        font-family: $font-family;
        font-weight: 400;
        font-size: 14px;
        text-align: left;
        color: $font-color-min;

        span {
          font-family: "Ostrich Sans", sans-serif;
          font-size: 18px;
        }
      }
    }
  }
</style>
