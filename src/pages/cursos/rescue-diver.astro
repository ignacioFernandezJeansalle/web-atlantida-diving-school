---
import { info, cursos } from "../../data/data.json";
import Layout from "../../layouts/Layout.astro";
---

<Layout
  title=`${cursos.rd.name} | ${info.name}`
  description=`Curso ${cursos.rd.name} - ${cursos.rd.description} | ${info.address}, Tel.: ${info.phone}, mail: ${info.email}`
>
  <section>
    <h1>RESCUE DIVER</h1>
  </section>
</Layout>
